# Remote E2EE v2 verification — 2026-09-13

## Scope and environment

Implementation branch: `claude/remote-e2ee`. Integrated baseline at this pass:
`05da063a`, then `d8cafddc` (headless Desktop integration). Windows x86-64,
Rust 1.97.0, Node v26.4.0.
No production NATS configuration, production agent, account credentials or paired
phone was changed. Tests use isolated homes, in-process fake brokers and public
fixture keys. Protocol and threat boundary: [REMOTE_E2EE.md](../../desktop/DEV_MD/REMOTE_E2EE.md).

## Completed local checks

- New Rust crypto crate: fmt, all-targets clippy with `-D warnings`, 6 tests pass.
- Desktop backend: fmt and all-targets clippy with `-D warnings` pass.
- Desktop remote/remote-host related Rust tests: 205 pass (including authenticated
  requests through the real command loop and encrypted presence/disconnect).
- Desktop TypeScript: typecheck, ESLint, all 110 Vitest suites / 967 tests pass.
- Mobile: typecheck, ESLint, all 71 Jest suites / 896 tests pass.
- Android Metro/Hermes export passed before the final origin/main integration;
  export is a bundle/compiler check, not a physical-device runtime test.
- Shared deterministic XXpsk0/IK vectors match Rust/snow and the Mobile backend:
  handshake messages, remote key, handshake hash, request/reply bytes and AAD.
- Adversarial tests cover altered bytes, wrong subject/key/PSK/prologue, replay,
  reflection, nonce exhaustion, replay-window wrap, old connection rejection,
  lost pairing confirmation, stale credential writes, failed persistence,
  invitation expiry/reuse, candidate readiness and stop invalidation.

## Windows full-suite baseline failures — NOT a green full local Rust run

The full desktop `cargo test` was attempted. Besides then-fixed v2 fixture updates,
it failed in unchanged Windows path/Git fixtures and exceeded the command time
budget before printing a final full-suite summary. A clean, detached worktree at
`05da063a` independently reproduces:

- `git_review::tests::git_output_returns_stdout_and_errors_on_failure`:
  `git_review.rs:600`, `File::set_modified` on a read-only-opened handle returns
  Windows error 5 (`PermissionDenied`).
- `commands::files::tests::best_effort_canonical_resolves_each_shape`:
  expected `/definitely/not/here/x/y/z.txt`, got the Windows canonical
  `\\?\D:\definitely\not\here\x\y\z.txt`.
- The `canonical_or_raw_canonicalizes_existing_and_falls_back` Git fixture also
  reproduces the same `set_modified` error on that clean baseline.

Those files are unchanged by the E2EE branch. This is evidence for those specific
baseline failures, not a blanket claim that every full-suite failure is harmless.
The repository's Linux test and three-platform compile CI must be checked before
opening the PR. Do not describe the 205-test scoped pass as a full desktop Rust pass.

## Desktop-JS record microbenchmark

Command: `npm test -- --runTestsByPath src/remote/__tests__/secureChannel.perf.test.ts`
under `mobile/`. Node `perf_hooks` timing, 100 warm-up records per case; each sample
includes both encryption and authenticated decryption, but no networking, JSON,
UI, IPC bridge or handset energy measurement. Keys are fixed public test keys.

| Plaintext | Samples | p50 | p95 | Extra wire bytes |
| --- | ---: | ---: | ---: | ---: |
| 128 B | 2,000 | 0.068 ms | 0.133 ms | 44 |
| 1 KiB | 1,000 | 0.086 ms | 0.191 ms | 44 |
| 64 KiB | 100 | 1.597 ms | 2.227 ms | 44 |

These are one local Jest/Node measurement, not Android/iOS latency guarantees.
Replay tracking uses a fixed-size ring for constant-time checks rather than
scanning the 4096-record window on every streaming update.

## Pre-PR CI and headless integration

Full manual CI passed **before PR creation** on implementation commit
`bba262646c2ef3a0c61b9721c88b429baaaf6d6c`:
[Actions run 34752393892](https://github.com/futuregene/future-os/actions/runs/34752393892).
This was a full manual dispatch, not a path-filtered no-op. It passed Linux Rust
fmt/clippy, all Rust test groups (including Desktop and the new crypto crate),
proto freshness, Desktop/Mobile JS checks, and Windows/macOS/Linux compile checks.

The headless Desktop changes that landed during implementation were merged and
adapted: its terminal QR/copy path now uses the same complete v2 invitation rather
than wrapping it into a v1 code. Additional local verification passed GUI and
headless all-targets clippy, 194 headless remote-related tests, and all four
headless CLI integration tests. This does not change the historical Windows
full-suite baseline failures described above.

## Remaining validation / release constraints

- Real Android/iOS pairing, suspend/resume, reconnect and file-transfer smoke tests
  have not been performed; no handset performance or battery claims are made.
- Production NATS TLS configuration has not been verified. The new Desktop build
  rejects a non-TLS deployment instead of silently downgrading.
- This is not an independent cryptographic audit or penetration-test certificate.
- The npm audit report contains five advisories in existing color/Metro/image-size
  dependencies; none of the reported advisory paths is a new crypto dependency.
