# Dimension matrix — where each dimension's evidence lives

Generated by `.future/cov100/gen-final-docs.py` from the workers' own module
docs (`docs/testing/module-*.md`, `desktop-*.md`). Every cell quotes evidence a
worker wrote in its own doc; an empty cell means that module recorded nothing
for that dimension, which is a gap for the reviewer to chase rather than a
claim — the honest form, because the line gate cannot see boundaries at all.

Dimensions: `boundary`, `error-path`, `concurrency`, `property`, `platform-cfg`, `serialization`

| module | dimension | evidence (quoted from the module doc) | doc |
|---|---|---|---|
| future-agent | `boundary` | The Choice option cap is a real boundary, not a comment: `a_catalogue_larger_than_the_choice_limit_is_clamped_to_255_options` sends 300 candidates and asserts the request carries exactly `MAX_CANDIDATES + 1 = 255` options, that … | module-agent-core.md |
| future-agent | `error-path` | The transport matrix is closed over the failure modes a caller must tell apart: non-2xx with Jev's own body, 2xx with unreadable JSON, connection refused, a peer that never answers (deadline), and a request reqwest cannot even … | module-agent-core.md |
| future-agent | `concurrency` | `abort_all_sessions_aborts_and_cancels_pending_approvals` builds a real `ServerSession`, starts a real `ApprovalGate::request` on another thread (a `shell` call from the Manual tier blocks until the gate decides), waits until … | module-agent-core.md |
| future-agent | `property` | Invariants with a stated failure condition, not just samples: the metadata-block scan (blank lines skipped, the first dedent ends the block, a block that runs out is a miss), `CostSplit` set iff something was priced, … | module-agent-core.md |
| future-agent | `platform-cfg` | Where the code is Windows-only, the tests are too, and they carry the mechanism in the comment: `--reset-windows-sandbox` runs sessionless against an isolated home; a corrupt `<home>/windows-capabilities.json` drives both cleanup … | module-agent-core.md |
| future-agent | `serialization` | typed `ContextMessage` projection (camelCase `runId`/`blocks`/`metadata`, run id lifted out of metadata, other keys preserved); `MessageBlock` `text`; skills `InstalledSkill` JSON (`id`/`scope`/`source`/`version`/`location`); … | module-agent-rpc.md |
| future-channels | `boundary` | `transport::text` — `blank_input_produces_no_chunks`, `exact_limit_is_one_chunk`, `every_chunk_respects_the_limit`, `splitting_never_breaks_a_character`, `astral_emoji_survive_splitting`, … | module-channels.md |
| future-channels | `error-path` | `providers::*` `an_unreachable_api_is_an_error_not_a_panic`, `a_malformed_*` (discord/mattermost/telegram/qq/wecom/whatsapp JSON), `a_bucket_429_is_retried_until_it_gives_up`, … | module-channels.md |
| future-channels | `concurrency` | `bridge::queue::tests::{different_conversations_run_concurrently, a_busy_conversation_is_not_evicted_for_a_new_one, a_full_mailbox_reports_backpressure, the_superseded_generation_*}`; … | module-channels.md |
| future-channels | `property` | `session_store::tests::{persists_and_reloads_from_disk, corrupt_disk_file_starts_empty, corrupt_source_is_preserved_when_memory_changes, save_failure_leaves_in_memory_state_intact}` (round-trip + invariant); `transport::text` … | module-channels.md |
| future-channels | `platform-cfg` | `test_support::kill_write_half` (unix `from_raw_fd` / windows `from_raw_socket`, **both branches compiled and one exercised per platform**); `providers::imessage` `platform_supported()` (macOS-only arms); `slack.rs` … | module-channels.md |
| future-channels | `serialization` | `config::tests` defaults/override/round-trip (`config.json` → structs, legacy Feishu/DingTalk top-level blocks vs `providers.<id>`); every provider's frame parsing tests (unknown op/event ignored, malformed JSON skipped, both … | module-channels.md |
| future-cli | `boundary` | `commands::skills` — device-name `COM1`/`COM10`, 128/129-character id, 200/201-character query, `pad` with CJK, a version-less catalogue row; `commands::configure` — 16 384/16 385-byte key, 256/257-character model id, … | module-cli.md |
| future-cli | `error-path` | `commands::auth` — 500 with/without a `message`, non-JSON body, empty granted key, terminal token error, unrepresentable and zero expiry; `commands::skills` — 500/404/invalid-JSON/unreachable fetch, invalid id and version before … | module-cli.md |
| future-cli | `concurrency` | `commands::auth::windows_device_code_flow` — the polling loop with `authorization_pending` then `slow_down` before the grant, and the mid-loop expiry break; `browser::windows_process` — the launcher call bounded by an inner … | module-cli.md |
| future-cli | `property` | `commands::skills` — `read_skill_md_version` table over the frontmatter grammar, `fetch_skills` round-trip of the catalogue shape, `list --json` document shape (`camelCase` keys, `null` for absent); `commands::session_history` — … | module-cli.md |
| future-cli | `platform-cfg` | `commands::auth::windows_device_code_flow` — the whole device-code flow on Windows via the unspawnable-URL technique, and `opener_command`'s exact `cmd /c start "" <url>` shape; `browser::safari::safari_manager` — the Windows … | module-cli.md |
| future-cli | `serialization` | `commands::auth` — the device-code and token payloads decode from the HTTP mock, and the saved `auth.json` is asserted key by key (existing `type` preserved, other providers untouched); `commands::skills` — the catalogue parses … | module-cli.md |
| future-loop | `boundary` | the append integrity boundary: the same `event_id` with **different** content fails closed (`StateEventConflictError`, `store.rs:1360`) while identical content is an idempotent no-op; `append_todo_change` refuses a non-todo event … | module-loop.md |
| future-loop | `error-path` | `compat::tests::write_active_state_reports_an_unusable_goal_dir_and_a_held_lock` + the two removal-refusal tests; `validator::tests::failure_carries_diagnostics`; `console_refusal_edges` (three tests, ~25 commands); … | module-loop.md |
| future-loop | `concurrency` | `compat::tests::concurrent_acquires_all_succeed` (4 threads × 150 rounds; the §2 fix); `validator::tests::cancelling_validation_reaps_the_owned_process` (unix); `tests/claim_lease_contract.rs` | module-loop.md |
| future-loop | `property` | the steering seam's no-progress arms, whose shared property is that an interrupt must never be LOST: a missing ledger, nothing appended since the offset, and a **partially written line** (no trailing newline) all return the OLD … | module-loop.md |
| future-loop | `platform-cfg` | the four `compat.rs` tests (Windows delete-pending vs unix `EEXIST`/`EISDIR`); `validator.rs` `#[cfg(unix)]` timeout/cancel/reap; `workspace_guard.rs` UNC/`\\?\` prefix handling (**OPEN**) | module-loop.md |
| future-loop | `serialization` | `tests/full_schema_contract.rs`, `tests/schema_alignment_contract.rs`, `tests/event_ledger_contract.rs`; `condensed_view_*` (unknown event type, malformed line, `usage` payload shape) | module-loop.md |
| future-rpc | `boundary` | the append integrity boundary: the same `event_id` with **different** content fails closed (`StateEventConflictError`, `store.rs:1360`) while identical content is an idempotent no-op; `append_todo_change` refuses a non-todo event … | module-loop.md |
| future-rpc | `error-path` | `compat::tests::write_active_state_reports_an_unusable_goal_dir_and_a_held_lock` + the two removal-refusal tests; `validator::tests::failure_carries_diagnostics`; `console_refusal_edges` (three tests, ~25 commands); … | module-loop.md |
| future-rpc | `concurrency` | `compat::tests::concurrent_acquires_all_succeed` (4 threads × 150 rounds; the §2 fix); `validator::tests::cancelling_validation_reaps_the_owned_process` (unix); `tests/claim_lease_contract.rs` | module-loop.md |
| future-rpc | `property` | the steering seam's no-progress arms, whose shared property is that an interrupt must never be LOST: a missing ledger, nothing appended since the offset, and a **partially written line** (no trailing newline) all return the OLD … | module-loop.md |
| future-rpc | `platform-cfg` | the four `compat.rs` tests (Windows delete-pending vs unix `EEXIST`/`EISDIR`); `validator.rs` `#[cfg(unix)]` timeout/cancel/reap; `workspace_guard.rs` UNC/`\\?\` prefix handling (**OPEN**) | module-loop.md |
| future-rpc | `serialization` | `tests/full_schema_contract.rs`, `tests/schema_alignment_contract.rs`, `tests/event_ledger_contract.rs`; `condensed_view_*` (unknown event type, malformed line, `usage` payload shape) | module-loop.md |
| future-tui | `boundary` | **`suggest_skill` with an empty candidate set and an empty query; a null/number/absent description; Unicode+CJK query and skill name**; **an OSC 8 URL with `BEL`/`ST`/newline/NUL/tab vs a Unicode URL**; **an incomplete escape … | module-tui.md |
| future-tui | `error-path` | **`suggest_skill` declines on a tonic `Unavailable` and on `success:false`, and on 6 malformed payload shapes**; **a draft file whose name cannot be created (not retried, path named)**; **a control-bearing link URL**; … | module-tui.md |
| future-tui | `concurrency` | **`prepending_history_shifts_the_viewport_and_notifies_the_owner` (callback fires exactly once)**; **`dropping_the_owned_agent_kills_its_child` (real child + external liveness observer, both directions)**; … | module-tui.md |
| future-tui | `property` | **the keys table: every documented spelling of a key → the same id, and an undocumented sequence → `None`**; **`set_compact_activity` idempotence and `rerender_message` on a folded member changing no rows (the fold invariant)**; … | module-tui.md |
| future-tui | `platform-cfg` | this module's work is the Windows half: the console harness makes `terminal_windows.rs` runtime-tested on Windows rather than type-checked only; clipboard and paste gained the Windows counterparts of their `#[cfg(unix)]` runner … | module-tui.md |
| future-tui | `serialization` | **`suggest_skill`'s payload shapes (typed decode with a JSON fallback, null skill, missing key, non-string name, non-string description, Unicode preserved)**; **the OSC 8 escape-sequence framing including the injection guard**; … | module-tui.md |
| desktop | `boundary` | `AgentThread.test.tsx`: a streaming bubble *before* the last user message is an older turn, not the current run; no thread / store still loading; a retry whose trigger id is unknown must not resend a different message; 9 tool … | desktop-agent.md |
| desktop | `error-path` | `MentionEditor.menus.test.tsx`: a failed workspace search closes the menu instead of leaving stale results; a superseded slow answer is dropped; a stale row click with no caret inserts nothing and throws nothing; a newline and a … | desktop-agent.md |
| desktop | `concurrency` | `MentionEditor.menus.test.tsx`: a superseded debounced search cannot replace a newer result; a stale row click after the caret is gone does nothing. `Composer.tools.test.tsx`: the `/compact` tool is withdrawn while the compaction … | desktop-agent.md |
| desktop | `property` | **Not N/A — corrected this segment.** The earlier claim here was "N/A by construction: no `platform` fork anywhere in the subtree, jsdom exercises the DOM path only". The first half is literally true (there is no … | desktop-agent.md |
| desktop | `platform-cfg` | **Not N/A — corrected this segment.** The earlier claim here was "N/A by construction: no `platform` fork anywhere in the subtree, jsdom exercises the DOM path only". The first half is literally true (there is no … | desktop-agent.md |
| desktop | `serialization` | `MentionEditor.menus.test.tsx`: `getContent()`/`restore()` round-trip for text, file pills and skill pills; brackets in a label neutralized to parens; a whitespace/paren path angle-wrapped; `<div>`/`<p>`/`<br>` wrappers … | desktop-agent.md |
| mobile | `boundary` | `useSendMessage.test.ts` :: empty/whitespace draft sends nothing, override wins over stale state, zero-total progress stays indeterminate; `utils.test.ts` :: `formatCostCny` 0/negative/NaN/Infinity/1e-5/"less than a … | module-mobile.md |
| mobile | `error-path` | `useSendMessage.test.ts` :: `send_compacting`/`prompt_too_large`/non-`Error` rejection restore the draft; `useComposerDraft.test.ts` :: Android pending-picker recovery rejects → toast keyed on the error, non-`Error` → generic … | module-mobile.md |
| mobile | `concurrency` | `useComposerDraft.test.ts` :: a conversation switch mid-load discards the late load; `useAttachmentPicker.guards.test.ts` :: the pick lock rejects re-entry from all four entry points and releases after failure; `ZoomableImage` :: … | module-mobile.md |
| mobile | `property` | `skillRecoBudget.test.ts` :: the message hash equals an independent byte-wise FNV-1a over the platform's UTF-8 bytes, for ASCII/CJK/emoji/mixed, plus the wrong-encoding discriminators; `utils.test.ts` :: `plainText` as an … | module-mobile.md |
| mobile | `platform-cfg` | `useComposerDraft.test.ts` :: `Platform.OS === "android"` runs pending-picker recovery, `ios` never calls it; `utils.test.ts` :: `showToast` uses `ToastAndroid` on Android and the app dialog elsewhere, `deferPresentation` 350 ms … | module-mobile.md |
| mobile | `serialization` | `remoteJson.test.ts` :: plain JSON, gzip JSON, gzip footer pre-check, oversize refusal; `catalogVersion.test.ts` :: epoch/revision fence, same-epoch re-auth keeps it, malformed revisions never advance; `storage.test.ts` :: … | module-mobile.md |

## Reading this table

* `boundary` — limits, empty/oversized input, off-by-one at a cap.
* `error-path` — what happens when a dependency fails (the half line coverage
  hides most often, since error branches are the ones tests skip).
* `concurrency` — interleavings, races, ordering, cancellation.
* `property` — invariants asserted over many inputs rather than one example.
* `platform-cfg` — code that only compiles/executes on one platform; see
  `platform-coverage.md` for what this host cannot measure at all.
* `serialization` — round-trips, wire/schema compatibility, encodings.

## Verification of this table

Because the cells are auto-extracted, a cell could in principle match the
dimension name incidentally and quote something that does not exist — which
would be worse than an empty cell, because it would look like evidence. So
every backticked identifier in these rows was checked against the tree
(`.future/cov100/check-matrix-evidence.py`): 22 of 23 resolve to real code.

The one that does not is `console_refusal_edges` in the future-loop /
future-rpc `error-path` cells — it is a **prose label**, used in
`module-loop.md` to name a group of three tests, not a test name; the test it
refers to is `worker_tail_condensed_raw_and_refusal_edges`. The extractor copied
the label faithfully, so this is a docs imprecision inherited from the source
doc, not fabricated evidence.

Two structural notes: `future-rpc`'s cells are identical to `future-loop`'s
because both are measured from `module-loop.md` (the crate shares that waiver
doc), so rpc's row is not rpc-specific evidence; and `desktop` aggregates six
subtree docs (`desktop-*.md`, `module-tauri-*.md`), so its cells quote whichever
of those recorded the richest evidence for that dimension.

Cells with no evidence: 0 of 48.
