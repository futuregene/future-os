# fix-apps remediation evidence

## Result and identity

- Session: `20260911-123030-c18ec4`; worker: `fix-apps`; goal: `goal_bughunt_fix_20260911`; round-1 todo: `todo_a3b806e59841`; round-2 todo: `todo_807893558e98`.
- Exclusive worktree: `/Users/geilige/future-os/.worktrees/bughunt-apps`; branch: `claude/bughunt-apps`.
- **Coverage: 29/29 original apps findings plus round-2 cross-owner w10 BUG-1, w10 BUG-6 and w13 BUG-6 have dispositions below.** Round 2 additionally closes the CI execution-path work for w31 BUG-3/offline scripts. No SF/NF extras assigned. Scope excludes `packages/rpc`; round 2 expressly permits minimal agent builtin-modality changes.
- Local implementation/checks complete, **external/native validation gaps remain**. Do not interpret the count of changed findings as 26 confirmed production bugs: it includes documentation/purity corrections and a replay guard whose end-to-end occurrence remains unverified.
- Read the contract first; reviewed REPORT, CROSS-MODEL-VERIFICATION, w27–w31, relevant V4, GLM-b7/b8 and KIMI sections. Reports are read-only inputs under `/Users/geilige/future-os/.future/bughunt`.
- Implementation commit: `6ae8f550467ccd067ca080d2f74ceb26285c786c` (55 files; includes this coverage ledger). A following evidence-only commit records this hash. No push, PR, branch merges, extra workers, or changes to main.
- Checkpoint: implementation committed at 13:17 local, about 47 minutes after launch, before the requested 50-minute partial-commit boundary. Temporary validation configs, dependency symlinks, generated mobile version and Python bytecode were removed; `git status --short` was clean immediately after the implementation commit. External/native gaps below are preserved, not represented as completed validation.

## Round 2 — authorized cross-owner continuation

Todo `todo_807893558e98`, authorized start epoch `1789108361`, deadline `1789115561` (additional 120 minutes; supersedes first-round checkpoint). Original 29-row slice remains accepted locally and unchanged except any explicitly listed follow-up. Current status: **LOCAL IMPLEMENTATION DELIVERED; native CI and legacy-data limitations remain explicit below.** First round-2 validation: `python3 scripts/test-generate-models.py` PASS (3 tests, including deterministic metadata from all three source schemas and legacy/empty-output distinctions). Generator/catalog reader modifications do not change `agent/src/models/builtin/models.json`. Inspection found `get_default_model_with` could choose non-text output before replacement_model's own filter; the minimal w10-6 patch therefore also filters text output in global-default selection. Exact overlap with fix-agent: `agent/src/models/mod.rs` conversion/default-selection/tests and `agent/src/models/builtin/mod.rs` output schema. No other agent paths changed.

Round-2 milestone (15:12 local): serialized `check-rust.py future-agent` **PASS** for fmt, clippy all-targets/-D warnings and full cargo test: 1715 library tests pass (1 ignored), 42 integration tests pass (23+1+1+3+14; 9 native sandbox smoke tests ignored), 0 doctests. New real builtin conversion/replacement regression passes. Desktop targeted identity/hook tests 4/4 + tsc/eslint PASS; mobile identity/controller/outbox tests 61/61 + tsc/eslint PASS. This is this worker's branch against the original agent baseline, not acceptance of fix-agent's separate branch.

| Cross-owner / follow-up ID | Final local disposition and evidence | Implementation/regression | Gap / integration note |
|---|---|---|---|
| w10 BUG-1 | **First-party caller defect fixed.** Catalogue id is provider-local, not prequalified; mobile incorrectly collapsed ids like provider=openrouter/id=openrouter/auto. Desktop modelKey already qualified correctly, but legacy modelOption stripped slash suffixes/chose first bare match and returned unqualified legacy defaults. | `bbc5efe1`: mobile always constructs full provider/id; actual controller set_model and first-prompt tests distinguish two providers with identical slash-containing ids. Desktop modelOption only accepts unique exact legacy ids, defaults and active hook emit full identity. Tauri checks the actual provider prefix instead of treating every slash as qualification. New helper/hook and real Rust bridge tests cover raw-slash and already-qualified paths. | Agent qualified-first resolver remains fix-agent ownership; source read from its separate worktree. Legacy redundant provider+model strings that already start with provider cannot distinguish a raw id from a qualified id without a protocol discriminator; current clients now always send full raw-id qualification, so do not guess/rewrite old ambiguous intent. |
| w10 BUG-6 | **Generator/reader pipeline fixed; subsequent authorized shipped-data pass adds 158 verified output fields (see continuation below).** Explicit output metadata was discarded and mapping hardcoded text; default-model selection also bypassed replacement's non-text filter. | `6d96e393`: preserve models.dev modalities, architecture modalities/arrow and explicit gateway non-language type; serialize output; serde reader preserves non-text/empty output with legacy text default; actual conversion/default/replacement test rejects image/embedding/empty candidates. Python fixtures cover all source adapters. | The initial pipeline delivery preserved all catalog bytes. The later authorized source-backed pass addresses all 158 differing outputs among 3394 matches and preserves 432 unmatched identities explicitly; see shipped-data continuation/provenance files. No model-name/context-size guessing or other catalog-field refresh. Exact overlapping agent functions: builtin_models/model_from_builtin/get_default_model_with plus new test, and builtin::Model.output. |
| w13 BUG-6 | **No desktop/mobile caller mismatch found; stale comment corrected and boundary verified.** Desktop prompt_command sends Attachment paths and an empty images vector. Mobile uploads image bytes, sends uploadId references; remote claim_uploads materializes AttachmentInput paths; agent loads/encodes images into data URIs. | `bbc5efe1`: desktop test explicitly asserts images empty, image kind/path intact; mobile actual prompt test asserts uploadId-only references and no images field. Existing agent image-prep/data-URI tests passed in full agent suite. Removed obsolete encode_attachments/base64 caller comment. | Third-party ImageContent::Base64 wire semantics/proto documentation remain fix-cli ownership. No speculative image/* MIME prefix added, no provider call or real image/secret read. |
| w31 BUG-3 / native scripts | **Native CI execution path implemented.** Script-only changes previously did not trigger a native regression. A standalone optional job would not necessarily be branch-protection-required, so that approach was rejected. | `.github/workflows/ci.yml` adds scripts/Makefile/builtin filters and offline Python + Windows PowerShell tests inside existing `Rust build (Windows)` / per-platform statuses, with no job-level skipping or renamed required checks. Script-only changes checkout/run tests without Rust compile. YAML structure, trigger inclusion and unchanged job/check names validated locally; three Python regression scripts PASS. | Native Windows execution is not claimed before supervisor opens the final PR and CI actually runs. No Windows/PowerShell installation on this Mac. |
| w30 BUG-3 | **Unresolved external TTL, unchanged.** | No secret/token capture and no speculative timer change. | Platform expiry policy absent. |

### Round-2 full checks and commits

- Implementation commits: `6d96e393` (generator/builtin output metadata) and `bbc5efe1` (qualified caller identity/image boundary). Final CI/additional regression/evidence commit is named in the handoff.
- Desktop final full `tsc --noEmit`, `eslint 'src/**/*.{ts,tsx}'`, Vitest: **100 files / 866 tests PASS**.
- Mobile final full `tsc --noEmit`, `eslint .`, Jest: **44 suites / 651 tests PASS**.
- Agent full batch: `python3 /Users/geilige/future-os/.future/bughunt-fix/check-rust.py future-agent` PASS (fmt/clippy/full test; counts above). No narrow lib-only/serial substitute.
- Tauri full fmt/clippy all-targets/-D warnings/test batch PASS: **1138 library + 1 binary test**, 1 doctest ignored. Supplied check-rust.py excludes Tauri from accepted crate names, so a temporary validator used the **same shared batch lock, pinned toolchain, Cargo target and isolated check-home** to serialize this excluded crate; removed afterward. No redundant independent builds.
- Offline Python: generator 3 tests PASS, profiling 2 tests PASS, Android configuration 4 host/architecture subcases PASS. YAML parses; all preexisting job names retained; native offline installer is part of existing per-platform gate rather than optional-only reporting.
- No round-2 test failures or flakes were observed after required build prerequisites were present. The first-round Tauri failure history below is historical, not a claim of another round-2 failure.
- Remaining work for supervisor: reconcile overlapping agent functions with fix-agent, run the native Windows CI step on the final PR, retain legacy-catalog metadata and JWT TTL limitations rather than counting them as repaired production incidents. No branch merge/push/PR/new todo by this worker.

## Shipped-data continuation (todo_64d9f49b8ea7)

Within the unchanged 16:32:41 deadline, supervisor explicitly authorized credential-free reads of the existing three metadata endpoints and output-only edits for exact existing (provider,id) matches. Inspection at 07:44 UTC found **3394/3826 exact matches with output/type metadata, 432 unmatched, 3236 already effectively text-only, and 158 differing outputs**. All records remain in place; no price/context/URL/input edits or name-based inference permitted. New source evidence also exposed a generator bug: Vercel embedding entries advertise `type: embedding` alongside `modalities.output: [text]`; the explicit embedding task type must take precedence for chat eligibility. **Final result: 158 output-only additions applied; 110 source-confirmed non-chat entries now excluded, and 48 mixed text outputs preserved.** All 3826 records retain their exact order and all preexisting non-output values. The 432 unmatched (provider,id) pairs are frozen in `docs/bughunt/model-output-unmatched-20260911.json`, not guessed. `docs/bughunt/model-output-provenance-20260911.json` records each endpoint's retrieval timestamp, SHA256/byte count, matching rules, coverage and seven inspected source fixtures.

New evidence versus the previous delivery:
- This changes **actual shipped data**, not just generator fixtures. Report-named `vercel-ai/bfl/flux-2-flex`, `vercel-ai/google/veo-3.1-generate-001`, `vercel-ai/voyage/voyage-3-large`, `vercel-ai/openai/text-embedding-3-large`, and `poe/openai/sora-2` resolve through the real builtin loader with image/video/embedding output.
- `handle_cycle_model` had no output filter at all. Added the same text-output eligibility predicate used by listing/default/replacement. The real RPC regression starts before each named non-chat entry in the credentialed catalog and proves cycling skips it; it also verifies actual `list_models`, global default and replacement results. No synthetic catalog entries are inserted in this regression; credentials are isolated fixture strings.
- Gateway embeddings actually have `type=embedding` AND `modalities.output=[text]`; generator task-type precedence now handles that observed shape. Both synthetic and inspected source fixtures cover it.

Validation: serialized `check-rust.py future-agent` finished **PASS at 16:20:49**, including fmt, clippy all-targets/-D warnings, **1716 library tests + 42 integration tests**, 1 library stress/9 native sandbox tests ignored, 0 doctests. The named shipped-data regression passed. `PYTHONDONTWRITEBYTECODE=1 python3 scripts/test-generate-models.py`: **4 tests PASS** including real shipped catalog against inspected source fields. Structural audit passed: 3826 records unchanged in order/non-output content, 158 output additions, 110 non-chat (6 audio/45 image/35 video/24 embedding), and 432 unique unmatched keys all still unchanged.

Limitations: 432 absent identities cannot be certified by these snapshots, including retired Vercel Imagen and xAI image/video entries; they remain explicitly unclassified. Upstream metadata is the evidence, not a credentialed inference/API capability test. The prior blanket 'catalog unchanged' limitation below is historical and superseded by this authorized output-only patch. Frontends were not changed or re-tested in this continuation. Additional agent overlap is limited to `rpc/commands/settings.rs::handle_cycle_model` and its shipped-data regression in settings_tests.rs; prior models.rs changes were already delivered. No pushes/merges/PR or deadline extension.

## Finding coverage

Paths below are relative to this worktree. Test filenames are under the corresponding source directory unless fully qualified. “Fixed” describes the local implementation, not native end-to-end certification.

| ID | Disposition and source/reachability evidence | Change and regression | Remaining gap |
|---|---|---|---|
| w27 BUG-1 | **Fixed.** `ApprovalPrompt.confirmRule/confirmCapabilityRules` only reset deciding in catch. Successful decision can leave the old card mounted when approval refetch fails. A thrown parent refresh is NOT the stuck path (it already enters catch), correcting the original report. | Both handlers use finally. `ApprovalPrompt.test.tsx` keeps the actual component mounted after each successful rule-save path and asserts controls recover. | No live WebView failure injection; actual React interaction covered. |
| w27 BUG-2 | **Fixed.** Editable-target early return precedes Escape while rule input autofocuses. | Handle editor Escape before the editable guard; retain ordinary typing shortcut isolation. Same test focuses input, closes editor, then rejects only on second Escape. | None at component level. |
| w27 BUG-3 | **Fixed, narrowed to over-expansion.** `prepareSearch` used `all[0]` even when first candidate was newer. | Window begins at earliest possible matching outside message. `useMessagePagingHook.test.ts` searches u3 with u1/u2 outside and asserts u3, not u1, becomes first rendered. | Deliberately retain the user's revealed reading anchor on search close; resetting to latest would hide a navigated result. Searching the oldest candidate necessarily renders the contiguous suffix. No virtualization rewrite. |
| w27 BUG-4 | **Fixed for unchanged settled outputs.** ContextPanel reconstructs the tool array; resource dependencies used array identity. `commands/runs.rs::list_tool_outputs` reads canonical tool output. | Stable run/id/status/end key in `RunInspectPanel`; still allow active-tool poll refresh. `RunInspectPanel.test.tsx`: three equivalent allocations fetch once; changed end timestamp fetches again. | Active tools intentionally continue refreshing; this is not a ban on live output polling. |
| w27 BUG-5 | **Fixed.** `Composer.attachImageFiles` swallowed read/save failure and could overwrite size-error state with later successes. | Accumulate image rejections and display after attachment processing, while continuing other items. `Composer.paste.test.tsx` drives the real editor paste event with a rejected image read and observes filename/error. | Mixed file/image error aggregation beyond the assigned single-image failure is unchanged. |
| w27 BUG-6 | **Fixed eligibility boundary.** Only blank detection used trim; raw whitespace counted toward the two-code-point non-Han threshold. | Count trimmed code points; update `threadSearchQuery.test.ts` to reject a plus surrounding whitespace but retain Han and multi-character queries. | Literal query matching is unchanged; only eligibility is normalized. |
| w27 BUG-7 | **Documentation/dead-state correction; not a reproduced live overwrite.** All messagesGenRef references were writes/parameters, while `reconcileThreadHistory` actually compares request-time baselines. | Remove unused ref plumbing/increments from useThreadMessages/useRunReattach/useAgentThreadState; comments identify baseline protection. Existing reconciliation, paging and live projection tests pass in full desktop suite. | No new generation-fence behavior introduced; original claimed production overwrite remains refuted by baseline protection. |
| w28 BUG-1 | **Fixed, dev StrictMode scope.** Cleanup set mountedRef false, setup never restored it. | Setup restores ref; subscription is also inside install try/finally so listener rejection cannot strand busy state. `UpdatePage.test.tsx` renders StrictMode and tests successful invoke/unlisten plus failed subscription recovery. | Production StrictMode double-effect impact is not claimed; no real updater download. |
| w28 BUG-2 | **Fixed.** Thread title and truncate used UTF-16 slice at a non-BMP boundary. | Shared truncate now slices code points; deriveThreadTitle delegates to it. `objects.test.ts` covers non-BMP character at title boundary and exact-limit input. | IPC rejection vs replacement was not re-enacted in a WebView; never claim the original report's assumed corrupt SQLite write. |
| w28 BUG-3 | **Partially confirmed and fixed by site.** Logout can reject (Rust logout-down test confirms); upload/open and export/save dialogs were outside catches; community save had only finally. **Refuted agent-down refresh_skills subclaim:** commands/skills.rs returns Ok after best-effort bridge refresh. | AccountPage and CommunityEditionSection show caught errors; artifact dialogs enter existing error handling. `actionErrors.test.tsx` and `artifacts/dialogErrors.test.tsx` drive all four rejected promises. No meaningless catch added to refreshSkills. | No native plugin failure injection; mocked actual component boundaries tested. |
| w28 BUG-4 | **Purity correction, no demonstrated user-visible production defect.** AppShell's updater called another setter; currently idempotent. | Move overlay close outside updater; updater only toggles expanded. Full desktop checks pass. | No behavioral regression invented for a presently idempotent operation. |
| w29 BUG-1 | **Fixed.** Structured upstream/provider termination strings contain interrupted/cancelled and were classified as user abort before source codes. | run_error.rs prioritizes the two structured prefixes. `structured_termination_is_not_a_user_abort` tests both actual strings; existing user-abort/timeout assertions retained. | None at classifier level. |
| w29 BUG-2 | **Fixed.** Added patch-body text `++ b/fake.rs` becomes `+++ b/fake.rs`, overwriting map key used by git_review and shadow_review. | Track hunk entry/reset and accept +++ path only outside hunks. `hunk_content_cannot_override_file_path` also checks next-file header reset/rename. | Parser test uses valid unified patch text; no destructive real repository repro needed. |
| w29 BUG-3 | **Fixed wrong-location writes.** Desktop accepted empty/relative HOME while downstream app/auth/skills paths simply joined it. | home_dir_from accepts only nonempty absolute HOME then USERPROFILE. `home_tests::rejects_empty_and_relative_home_overrides` exercises fallback and rejection without mutating global env. | Preserve existing no-home => None/error behavior; unlike agent, no new OS-account-database fallback was added. Native Windows tests still recommended. |
| w29 BUG-4 | **Fixed input-validation mechanism; Windows exposure is test-platform builds only.** Raw target passed to Path::join after only '..' checking; Windows roots/prefixes replace the base. | Reject backslash and colon on every host before join. Actual local TCP `web_server_serves_files_and_rejects_bad_requests` covers drive/root/UNC/ADS targets returning 403, plus ordinary serving. | macOS verifies rejection before filesystem IO; native Windows exploitation was not performed. No real file/SMB target touched. |
| w29 BUG-5 | **Fixed.** macOS fallback `open -t path` lacked option terminator (unlike open crate). | Construct `open -t -- path`; command-argument regression checks option-like operand without launching an application. | No Calculator/application-launch repro. Existing full-suite nonexistent-path opener test also passed. |
| w29 BUG-6 | **Refuted independently.** config_io.rs increments TEMP_COUNTER each call; AlreadyExists propagates through closure to remove_file(tmp), so residual collision is at most transient, not permanent. | No code change. Existing atomic-write/error-cleanup tests pass. | Rejected suggested truncate/create replacement: it weakens create_new safety and is unnecessary. |
| w30 BUG-1 | **Mechanism repaired with explicit reachability qualification.** applyOps clears lane.ops then discarded gap suffix after a successful-but-empty replay. Agent-journal vs mirrored-event ordering is not proven end-to-end here. | Preserve all suffix ops; same-run unresolved replay uses existing bounded exponential retry machinery and diagnostic failure callback, rather than hot-looping or discarding agent_end/mutations. `syncEngine.test.ts` uses fake time and real engine: empty successful replay, no hot loop, journal-only catch-up, terminal streaming=false, retained mutation. | Need integration evidence about occurrence under actual journal/publish ordering. Not counted as a demonstrated production incident. Permanently unavailable gaps stay pending with capped retry delay. |
| w30 BUG-2 | **Fixed.** ChatScreen requests common.retry; both locale common maps lacked it. | Add en/zh keys; ComposerDock test file verifies both resources. | None at resource level. |
| w30 BUG-3 | **Unresolved external evidence, unchanged.** client.scheduleRefresh has no int32 ceiling; GLM narrowed overflow to Android, not iOS. Platform JWT TTL is absent from this repository; fixtures use short TTL. | No speculative production bug fix. | Supervisor needs platform-issued NATS JWT expiry policy (>24.85 days or not), without exposing a real token. Native timer behavior can then be tested if relevant. |
| w30 BUG-4 | **Fixed.** One string approvalError was passed to every pending card after submitting cleared. | Error carries request id; ComposerDock only passes it to matching card. `ComposerDock.test.ts` renders two cards and asserts only A receives A's error. | Native styling not changed; actual component props checked with mocked presentation leaves. |
| w30 BUG-5 | **Fixed reachable upload EOF stall; narrowed other claims.** Local file can shrink after transferSize capture; readBytes returns zero without offset progress. Real desktop chunkBytes is fixed positive, and download out-of-range errors terminate requests. | Reject empty upload reads; validate upload chunkBytes, cancel transfer, close handle. `files.test.ts` covers short source plus 0/-1/NaN/Infinity chunk sizes. | Invalid-protocol download infinite-loop claim is not a confirmed current desktop path; no speculative download rewrite. |
| w31 BUG-1 | **Fixed.** All three fetch failures left unique_models=[] and still wrote catalog/wiki with successful exit. | Raise before any output write when no models survive. `scripts/test-generate-models.py` invokes actual main with None/empty sources, preexisting catalog sentinel, and verifies no wiki generation. | Partial upstream availability semantics and crash-atomic output are unchanged; not the assigned all-failed bug. |
| w31 BUG-2 | **Fixed profile isolation.** Agent lock is per-user before profile service startup; a new port alone cannot isolate it. | `scripts/profile-isolated.py` wraps all Makefile profile targets and standalone bench/quick scripts; child HOME/USERPROFILE are disposable, discovery overrides cleared, child status preserved. Offline subprocess tests confirm environment/cleanup/nonzero status and reject real-home override. | Default profile has no user credentials/sessions; for model-load profiling use a separately configured absolute FUTURE_PROFILE_HOME. No live agent profiling was run. Windows wrapper invocation needs native smoke check; Python is required. |
| w31 BUG-3 | **Fixed by source contract, Windows execution pending.** install.ps1 pinned path kept v while release asset naming strips it; pinned checksum bypass lacked warning. | Normalize leading v/V and warn. `scripts/test-install.ps1` mocks download boundary for three version spellings; never installs anything. | PowerShell unavailable here, so this new offline test has NOT run. Native command: `powershell -ExecutionPolicy Bypass -File scripts/test-install.ps1`. |
| w31 BUG-4 | **Fixed.** Actual recursive converter crashes on ~2000 blockquotes; parsed tree itself can be inspected iteratively. | Iterative AST nesting guard (64) before recursive conversion/rendering; over-limit document becomes lossless plain text. `bughuntParsing.test.ts` calls actual package parser with 2000 levels and ordinary nested quotes. | Not an unlimited-input resource guarantee for remark itself; no speculative size cap. |
| w31 BUG-5 | **Runtime universal claim refuted; documentation corrected.** Correctly CommonMark-escaped UNC reaches the parser intact (new passing counterexample). A single leading backslash is also a legitimate Windows drive-rooted path, so promoting it to UNC changes target/authority. | Correct localPath docs to explain decoded href / escaping / file URI. `bughuntParsing.test.ts` round-trips a correctly escaped UNC through the real parser. | Original unescaped source necessarily follows CommonMark escaping. Rejected automatic single-backslash-to-network-share “fix” as unsafe/ambiguous. |
| w31 BUG-6 | **Fixed.** Number scanner lacked literal scanner's word boundaries and colored error_404/user2 suffixes. | Symmetric word-boundary checks in numberEnd. Actual tokenizer regression covers identifiers and valid integer/fraction/exponent tokens with lossless text. | Not intended to validate arbitrary malformed JSON; only token-boundary defect fixed. |
| w31 BUG-7 | **Fixed new emulator configuration.** ABI and SDK defaults were hardcoded to Apple Silicon/macOS. | Detect x86_64/arm64, Darwin/Linux SDK paths, respect ANDROID_SDK_ROOT, Linux javac home discovery. `test-android-config.py` runs actual shell script for four host/arch combinations with fake tools, deliberately stops at SDK boundary. | No SDK download/emulator launch. Existing incorrectly configured AVDs are not automatically deleted/migrated. macOS Homebrew users may still need explicit JAVA_HOME. |

## Round-1 validation results (historical)

Final frontend pass at about 13:15 local:

- Desktop: `tsc --noEmit` **PASS**; `eslint 'src/**/*.{ts,tsx}'` **PASS**; Vitest **98 files / 862 tests PASS**.
- Mobile: `tsc --noEmit` **PASS**; `eslint .` **PASS**; Jest `--runInBand` **44 suites / 647 tests PASS**.
- Tauri: pinned Rust **1.97.0**, `make desktop-sidecar-placeholder`, `ulimit -n 10240`, shared `CARGO_TARGET_DIR=/Users/geilige/future-os/target`; `cargo fmt --check` **PASS**, `cargo clippy --all-targets -- -D warnings` **PASS**. Final full `cargo test --quiet`, isolated HOME+USERPROFILE: **1137 library tests + 1 binary test PASS; 1 doctest ignored** (93 seconds).
- Preserve failure history: initial Tauri run exceeded a 120-second tool timeout. Next full run had **1136 pass / 1 fail**, `agent_bridge::pipeline_tests::reanimate_still_streaming_run_attaches_an_observer`: SQLite DatabaseBusy during fixture initialization at test_support.rs:462. No SQLite code changed; subsequent unchanged full rerun passed 1137/1137. Treat as observed test flake, not silently “fixed”.
- Python: `python3 scripts/test-generate-models.py` PASS (two source subcases); `python3 scripts/test-profile-isolated.py` PASS (2 tests); `python3 scripts/test-android-config.py` PASS (four host/arch subcases). `bash -n scripts/start-mobile-android.sh scripts/agent-profile-bench.sh` PASS.
- PowerShell installer test: **NOT RUN** (no pwsh/Windows PowerShell installed). No tools were installed to force platform evidence.

### Worktree dependency caveat and reproducibility

Existing dependencies were reused read-only from the main checkout. Temporary Vite/Jest configs mapped `@future-os/{markdown,thread-projection,json-preview}` to this worktree's source, so tests did **not** accidentally exercise unchanged ancestor workspace packages. These scratch configs/dependency links are removed before handoff. In a normally installed integration worktree, standard `cd desktop && npx vitest run` and `cd mobile && npm test` suffice. This worktree initially lacked generated mobile version.ts and future-share-intent dependency link; generated the normal version artifact and linked the local module for checks (no tracked package/dependency changes). Temporary generated/symlink/cache artifacts are removed before handoff.

## New evidence versus source audits / rejected approaches

- Added actual implementation/component tests rather than copied pseudocode: StrictMode install, both rule-save paths, focused Escape, real paste, four UI error boundaries, bounded replay recovery, upload EOF cancellation, parser depth, tokenizer, native command argument construction and HTTP handler.
- New UNC counterexample refutes “UNC can never survive markdown”; documentation was inaccurate, but silently upgrading a root path to SMB is not a safe fix.
- Confirmed w29 BUG-6 refutation and preserved create_new. Did not add pointless catches around best-effort refresh_skills, reset search to latest on close, remove live output polling, copy real credentials into profiling homes, or infer a production JWT TTL.
- Round 1 had no protocol/schema/API changes requiring another worker's source edits. Round 2 adds the backward-compatible builtin catalog output field and documents the exact models-module overlap above; no RPC schema changes. Shared markdown behavior still falls back to plain text only beyond the nesting limit; both desktop/mobile suites cover consumers. Supervisor should re-run scoped checks after integrating shared RPC changes from fix-cli.

## Remaining acceptance gaps / next useful checks

1. **External unresolved finding:** w30 BUG-3 platform TTL. Ask platform owner for expiry policy; no token content required. Until then no claim this is a reachable production defect.
2. **Native validation:** round 2 wires the offline PowerShell installer and Python profiling regression into the existing Windows CI gate; wait for actual final-PR execution. Tauri static-server native path validation and real x86 Android emulator remain platform checks, not claimed by macOS unit tests.
3. **Legacy catalog data:** the authorized continuation now patches 158 output fields and excludes 110 source-confirmed non-chat entries. The exact remaining 432 unmatched identities are preserved in model-output-unmatched-20260911.json; no invented annotations. Their unavailable/retired source metadata remains an evidence gap, not an asserted production defect.
4. **Integration qualifier:** exercise delayed journal availability vs mirrored mobile events if a deterministic backend integration seam exists; the local replay algorithm is tested, but original occurrence frequency/reachability is unproven.
5. Supervisor owns final combined PR, origin/main sync, CI/auto-merge and branch/worktree cleanup. This worker creates no successor todo and does not close the overall goal. Handoff uses `--no-follow-up`; gaps above remain explicitly with supervisor.
