# fix-cli finding ledger

Session `20260911-123030-f93dca`; branch `claude/bughunt-cli-channels`; base `fc81c016`. **DELIVERED FOR SUPERVISOR REVIEW — owned implementation and complete scoped checks passed**. This is not global-goal/PR completion. Round 1 started 12:30 CST; controlled stop preserved all files. Round 2 authorized 120 minutes from epoch 1789108361, deadline 1789115561 (2026-09-11 16:32:41 CST). Round 3 authorization (start1789127056/deadline1789134256) supersedes that historical window. Read CONTRACT, ROUND2, ROUND3, RUN and supervisor-review. No pushes/PRs/merges/additional workers.

Read-only evidence sources: `/Users/geilige/future-os/.future/bughunt/{REPORT,CROSS-MODEL-VERIFICATION,w21*,w22*,w23*,w24*,w25*,w26*,w32*,verify/V3,verify/V4,verify/KIMI,verify/GLM-b5,verify/GLM-b6,verify/GLM-b7,verify/GLM-b8}.md`. Source implementation and actual fixtures override historical report labels. **Implemented** below means code plus regression verified in the final scoped batch at 19:56:14 CST on 2026-09-11; this does not claim native-platform/live-service certification. 61 original rows + 3 V4 extras + round-2 w13-6.

## Finding coverage

Paths below are workspace-relative. Regression names are runnable with `cargo test -p <crate> <name>`; suites summarized below.

| Finding | Disposition and source evidence | Implementation / regression / remaining gap |
|---|---|---|
| w21 BUG-1 | Confirmed: `tools_call` never recognized documented --args | Implemented top-level JSON argument parsing and explicit flag overrides in `cli/src/commands/tools.rs`; recorded MCP body in `tools_call_input_mask_base64_and_file_type_lowercasing` now uses --args and asserts no nested args. |
| w21 BUG-2 | Confirmed platform-independent error-kind bug | Implemented ErrorKind::NotFound before string conversion; `load_api_key_resolution_order` covers absent auth; native Windows diagnostics not executed. |
| w21 BUG-3 | Confirmed: formatter unconditionally claimed saved after decode/write failure | Implemented Result propagation through image/tool formatter and CLI exit; bad-base64, blocked-parent tests. No false success path remains. |
| w21 BUG-4 | Confirmed: seconds*1000 unchecked | Implemented checked u64 conversion; `timeout_overflow_and_paper_limit_are_rejected`. |
| w21 BUG-5 | Confirmed: parent expect panics for root/empty | Implemented filename validation; `format_image_result_root_output_path_has_no_parent` now uses real b64_json (old test mistakenly used base64, never reaching save). |
| w21 BUG-6 | Confirmed: whole-path rfind dot uses parent extension | Implemented Path file_stem/extension; `image_output_parent_dots_and_write_errors`. |
| w21 BUG-7 | Confirmed: messages containing 1401/4403 misdiagnosed | Implemented complete numeric token matching; `translate_error_tool_specific_and_default` covers four counterexamples. Still heuristic for genuine standalone numeric error text, not a structured HTTP API redesign. |
| w21 BUG-8 | Confirmed: advertised 1..20 limit absent | Implemented int_range; timeout/paper-limit regression. |
| w22 BUG-1 | Confirmed silent connection hang; reject proposed overall 5-minute cutoff because current transport explicitly supports long runs | Implemented 300-second inactivity watchdog, reset by each event; `silent_event_stream_times_out_instead_of_hanging` uses silent mock and 50ms test timeout. Healthy pinging long runs deliberately remain unbounded. |
| w22 BUG-2 | Confirmed stream Err was normal success | Implemented error propagation and corrected misleading parity comment/test; `stream_events_mid_stream_error_is_reported`. |
| w22 BUG-3 | Confirmed empty key presence accepted | Implemented empty-key filtering in auth/account and rejection of empty OAuth token; `get_future_auth_entry_edge_cases`; CLI account empty-key rejection regression added; no native Windows execution. |
| w22 BUG-4 | Confirmed absent balance falsely became zero | Implemented absent profile keys omitted and unknown credits null/NaN; `balance_missing_field_is_unknown`; full check PASS. |
| w22 BUG-5 | Confirmed reqwest has no default 30s timeout | Implemented explicit 30s request timeout covering body in auth; existing mock login tests; dedicated `auth_request_timeout_includes_response_body` exercises actual 30-second request timeout. |
| w22 BUG-6 | Confirmed unchecked u64 arithmetic; original report incorrectly says huge elapsed remains less than expiry | Implemented monotonic Instant, checked expiry, Duration seconds (no multiply), bounded sleep by remaining authorization life. `login_rejects_extreme_expiry_and_empty_granted_key` covers u64::MAX expiry and empty granted key without real opener/network. |
| w23 BUG-1 | Confirmed var closes over last console level/method | Implemented let bindings in actual embedded script; `node cli/tests/console-hook.mjs` executes source, PASS at 14:47. |
| w23 BUG-2 | Confirmed href fallback incorrectly navigates current tab after _blank/download/slow native action | Removed synthetic href navigation rather than guessing tab targets; mock regression `click_href_does_not_synthesize_navigation_when_native_action_is_unobserved`. Native browser not rerun this round. |
| w23 BUG-3 | Confirmed headers-only timeout excludes body | Implemented request timeouts in endpoint, browser status, Safari status and WebDriver fetch; `response_body_is_covered_by_timeout` added; two legacy diagnostic race expectations corrected. |
| w23 BUG-4 | Confirmed explicit navigation and ActionNavigationObserver dropped unsubscribe handles without executing them | Implemented unsubscribe on completion/dispose/re-arm; observer regression dispatches after dispose and asserts state unchanged. |
| w23 BUG-5 | Confirmed quoting omitted tab | Implemented whitespace predicate and tab/newline tests in windows_process; native Windows launch not executed. |
| w23 BUG-6 | Confirmed float/port narrowing | Implemented finite integer/range checks before cast; `tabs_error_paths` covers fractional index and invalid ports. |
| w23 BUG-7 | Confirmed negative-half rounding diverged from promised JS semantics | Implemented floor(center+0.5) in input and click; negative-half center test. |
| w23 BUG-8 | Confirmed lowercase Safari keys and unescaped text XPath | Implemented case-insensitive key map, changed mock press to lowercase; added quote-safe XPath literal builder; quote-literal regression covers both quote kinds; native Safari not executed. |
| w24 BUG-1 | Confirmed UTF-8 approval preview panic | Implemented floor_char_boundary; real approval-card test now contains CJK+emoji >500B; duplicate w32-2. |
| w24 BUG-2 | Confirmed DingTalk had no admission before full-permission session creation | Implemented sender_allowlist, default empty/deny; explicit ["*"] opts into trust-all. Applies to DM and group senders before slash/prompt. `admission_denies_unlisted_senders_before_slash_or_prompt`. Operator migration must populate sender IDs. |
| w24 BUG-3 | Confirmed remote filename joined as path; write errors swallowed | Implemented portable single-filename validation, unique UUID-prefixed create_new saves, propagated failures. Tests absolute/traversal/Windows separators/ADS/root/empty/duplicate uploads/blocked parent. Duplicate w32-1, V4 N3. |
| w24 BUG-4 | Confirmed every ensure_session reset model/effort | Defaults only initialize new sessions; `session_defaults_are_not_reapplied_after_model_change`. Existing session reactivation remains. |
| w24 BUG-5 | Confirmed real card events omit chat_type, and current-session lookup loses thread | Implemented server-side approval ID→origin chat/type/session binding at actual approval event. Policy uses bound group/DM context, refuses unbound/cross-chat IDs; `group_thread_card_uses_origin_policy_and_session` includes missing chat_type and reset thread mapping. Restarted bridge safely refuses old unbound cards (not silent success). |
| w24 BUG-6 | Confirmed card message ID is not click event identity; original proposed mutable decision semantics rejected | Card dedup uses header event_id, fallback message/sender/action body. Same card with distinct requests regression in `card_action_approve_and_reject`; delivered route removed, repeat decision cannot change an already accepted approval. |
| w24 BUG-7 | Confirmed Error and AgentEnd(error) skipped card finalize | Implemented settings(false) then full content update on both error paths; `error_finalizes_existing_stream_card`. Also repaired abrupt EOF/transport cleanup: stop streaming, publish partial text, remove typing, and propagate transport errors. `abrupt_stream_end_cleans_card_and_reaction` covers clean EOF and mid-stream failure. |
| w24 BUG-8 | Confirmed ToolEnd mutation incorrectly depended on existing CardKit card | Implemented local marker replacement regardless of card; `tool_first_completion_and_nonstreaming_thinking`. |
| w24 BUG-9 | Confirmed early mention gate disabled configured false | Pass actual mention flag to policy; silent denied nonmentions retained; `group_without_mention_respects_disabled_requirement`. |
| w24 BUG-10 | Confirmed setup/transport errors invisible; stream semantic errors already replied | Implemented webhook error replies for spawn failure and session setup failure; corrected `prompt_failure_is_logged_not_raised` to require Error reply. |
| w24 BUG-11 | Confirmed all HTTP statuses ignored; real provider failure schema not observed | Implemented HTTP error_for_status and nonzero errcode rejection; `webhook_rejects_http_and_application_failures` tests 503 and synthetic 200/310000. Native DingTalk integration not claimed. |
| w24 BUG-12 | Confirmed negative expiry cast makes enormous Instant add | Implemented saturating subtraction, nonnegative expiry and checked Instant add; `token_short_or_negative_expiry_does_not_panic` covers 30/0/-1/i64::MIN. |
| w24 BUG-13 | Confirmed superseded stream leaves typing | Implemented remove typing and stop old card streaming on supersede; existing superseded test updated; stale-arrival test separately asserts reaction deletion; existing supersede test confirms card settings shutdown. |
| w24 BUG-14 | Confirmed dead image-limit/typing/name config | Implemented content-length + chunk byte limit before Vec growth (all attachments), typing flag, sender-name resolution for authorized message logging. `download_resource_ok_and_http_error` includes limit and image-post test disables typing. `chunked_resource_is_limited_without_content_length` covers the streaming counter without a length header. |
| w24 BUG-15 | Confirmed image-only post sent through file_key parser | Implemented post img key extraction + image handler reuse; `image_only_post_is_downloaded_and_typing_flag_is_respected`. Only first post image supported; multi-image post semantics not expanded. |
| w24 BUG-16 | Confirmed unconditional Approved on missing/failed session | Implemented truthful not-delivered acknowledgment and origin routing; existing no-session/RPC-failure tests corrected. |
| w24 BUG-17 | Confirmed thinking ignores streaming false | Implemented shared streaming predicate; combined tool-first/nonstreaming-thinking regression. |
| w25 BUG-2 | Fixed: both WS callbacks now synchronously enqueue per-conversation arrivals | Weak-owned queue consumers serialize only setup; generations assigned before stream-task spawn, stale generations rejected under start lock. Feishu slow-old-ACK queue test and both channel stale-generation tests pass. Response streams remain concurrent; independent chats have separate queues. Round3 bounds each queue to128 events, returns false and warns on overflow; both `ingress_queue_has_a_fixed_capacity` tests verify rejection of event129. Overflow policy is documented, not silently represented as executed work. |
| w25 BUG-4 | Confirmed HashSet iteration is not oldest | Implemented VecDeque FIFO bounded window in BOTH channels. Feishu bounded-dedup regression asserts oldest retained=500/newest=1004 after1005 arrivals; suites cover repeated delivery. |
| w25 BUG-5 | Latent, no current production reentrant handler found | Router dispatch holds mutex and callbacks only mutate own state today. Callers in chromium_page/chromium_navigation/execution_context closures update their own state, never router add/unsubscribe/clear. No production reentrant path: retain as latent API constraint, no code change or production-fix claim. |
| w25 BUG-7 | Fixed: browser config direct write and stale read/modify/write | OS file lock now covers all eight production read/modify/write sites. Atomic tempfile persist publishes complete JSON; no await between write/publish avoids cancellation-released-lock writer race. Concurrent 20-update fixture retains all refs; existing load/save tests pass. |
| w25 BUG-8 | Confirmed send precheck races disconnect drain | Implemented recheck while holding pending mutex; added concurrent-send/disconnect regression (stress schedule, not a deterministic forced old window). |
| w26 BUG-1 | Fixed protocol parity; no claim of prior data-first consumer data loss | Additive optional ToolStart phase4/index5, ToolDelta snapshot4; encode/decode preserve true/false/absent. Actual agent-wire fixtures serialized through prost; frozen old decoder compatibility test passes. CLI bridge-consumer parity integration added. |
| w26 BUG-2 | Fixed protocol parity; dual-write contract retained | AgentEnd truncation6, UsageEvent stop_reason2, UsageInfo reasoning7/provider_metadata8 appended. Producer-wire fixture and actual future_agent::types::Usage integration cover metadata and cost serialization. No field-number reuse. |
| w26 BUG-3 | Implemented meaningful final-page nextOffset preservation | `final_entries_page_retains_pagination_metadata`; unpaged and paged-empty/offset0 remain indistinguishable in current nonoptional proto. Historical report omitted valid unpaged response. This residual default-key ambiguity has no value-level loss; no breaking optional-scalar API change. |
| w26 BUG-4 | Confirmed explicit custom shell RPC duration contract exceeds fixed 125s; built-in callers default | Implemented dynamic request timeout matching server clamp +5s; `shell_deadline_tracks_clamped_execution_timeout` tests default/short/10min/max. |
| w26 BUG-5 | Confirmed chmod before ownership check can mutate foreign dir when privileged and obscures denial unprivileged | Reordered metadata ownership before chmod; `foreign_socket_directory_is_rejected_before_chmod`, safely skips root. |
| w26 BUG-6 | Implemented opaque retention for malformed imported image block | `final_entries_page_retains_pagination_metadata` also asserts raw image metadata preserved; full session-entry proto encode/decode now asserts raw malformed-image metadata retained. |
| w26 BUG-7 | Latent/contract review: malformed legacy entries rejected wholesale; same-version producer uses strict canonical entries | Refuted as current supported-path defect: `agent/session/display.rs::project_entries` emits canonical SessionEntryPayload and decoder intentionally deny_unknown_fields; existing same-contract test rejects legacy content. No row-dropping fix: silently losing history is worse than explicit incompatibility. Latent old/foreign-agent compatibility remains. |
| w26 BUG-8 | Latent/corrupt-input behavior; byte-stable data contract makes suggested JSON accessor fallback unsafe without consumer proof | No change: all in-repo producers serialize valid JSON before assigning data; strings preserve authoritative journal bytes. Corruption/foreign-writer fallback is a latent recovery-policy request, not a reachable same-version producer defect. Changing raw accessors would violate explicit byte-stability contract. |
| w26 BUG-9 | Latent forward-compatibility: current enum only existing/running/queued; unknown→Running is questionable | No new production state exists (RunAcceptedState only Existing/Running/Queued; scheduler constructors match). Retained latent compatibility concern, not claimed repaired production behavior; no new enum/API or speculative default semantics. |
| w26 BUG-10 | Confirmed empty error differs typed/JSON | Implemented common unknown-error interpretation and preserved error key in typed reconstruction; parse-both-twins and empty error parity pass. |
| w26 BUG-11 | Refuted production ambiguity: accepted_state=queued explicitly distinguishes sentinel epoch0 from running epoch0 | No consumer currently fences queued epoch; document epoch validity rather than breaking proto scalar API. Proto and RunAck docs now explicitly require checking accepted_state before using epoch as a fence. |
| w26 BUG-12 | Latent: run_terminal producer always writes usage | Source search: `agent/src/rpc/mod.rs` obtains requestedRun only via `future_rpc::message::run_terminal`, which always emits usage. Mechanism needs out-of-contract producer: latent, no code change or double-write restoration. |
| w32 BUG-1 | Duplicate confirmed w24-3 | Same implementation/tests; not counted as distinct bug. |
| w32 BUG-2 | Duplicate confirmed w24-1 | Same implementation/test. |
| w32 BUG-3 | Latent, dormant DingTalk AI-card module | `stream_ai_card` has no bridge caller; bare byte slice exists but no production impact today. No speculative activation/refactor. |
| w32 BUG-4 | **Non-owned confirmed** `agent/src/llm/sse.rs::push` drains/memmoves suffix per newline | Supervisor/agent handoff: scan complete lines using cursor and drain prefix once per push, preserve split UTF-8/CRLF and event bounds; test actual decoder with many short lines. No agent edit in this branch. |
| w32 BUG-5 | **Non-owned confirmed, limited** loop page goal IDs are CLI-controlled and injected into inline onclick via encodeURIComponent (apostrophe remains) | Supervisor/loop handoff: replace inline JS with data attributes/delegated handler, or proper JS-string+HTML escaping. Locations `orchestration/loop/src/webui/page.rs` 370/381; todo-ID 537/713 currently generated and latent. No loop edit here. |
| V4 N1 | Same corrupted-data family as w26-8; raw JSON accessor deliberately promises verbatim data | Refuted independent bug: raw accessor promises verbatim bytes, parsed accessor cannot represent invalid JSON; same valid producer inputs agree. Corrupt-input recovery policy remains latent w26-8, not a production-fix claim. |
| V4 N2 | Duplicate empty-error key issue w26-10 | Implemented serialization retains empty error key; empty-error parity passes. |
| V4 N3 | Duplicate save failure w24-3 | Implemented errors propagate; no successful saved path on failure. |
| w13 BUG-6 (round 2) | Fixed documentation mismatch, not a first-party runtime defect | Inspected agent grpc Base64→ImageContent and prompt_helpers image_url construction plus channel data-URI producer. Proto Base64 field11 and generated docs now explicitly require complete MIME data URI, not raw bytes. No field renumbering or guessed MIME conversion; apps/agent runtime boundary remains owned by their workers. |

## commits

- `dc642044`: preserved initial CLI/channel/RPC repairs and ledger.
- `d9bff2ab`: ordered channel ingress/start handoff, FIFO dedup, browser OS-locked transactions and atomic config writes.
- `7e621a5c`: additive event wire metadata, regenerated proto, old/new compatibility and producer-consumer fixtures.
- `c7bb4ea5`: final EOF/error cleanup, auth/limits regressions, configure/env isolation fixes, and English/Chinese config migration docs.
- `fbda3cce`: bounded128-event conversation queues, overload rejection regressions and documented saturation behavior.
- Worktree is clean after code commits; this worker's generated `target/test-homes` was removed (shared build target untouched). Final report-only commit is listed in the handoff.

## coverage

All **61 assigned originals**, **V4 N1/N2/N3**, and cross-owner **w13 BUG-6** have individual current dispositions above. Duplicate IDs are not distinct production bugs. No current confirmed owned finding is silently omitted; non-owned w32-4/w32-5 are explicitly handed to supervisor/loop worker. Latent unsupported-producer/reentrancy cases are not counted as repaired production defects.

## tests

**PASS:** `python3 /Users/geilige/future-os/.future/bughunt-fix/check-rust.py future-rpc future-channel future-cli` completed 2026-09-11 **19:56:14 CST**, exit0, using pinned1.97.0, serialized target batch, isolated HOME, full `fmt --check`, `clippy --all-targets -- -D warnings`, and full `cargo test -p` for all three crates.

- future-rpc: **115 unit + 1 additive compatibility + 5 wire integration**, all pass; doc tests pass (0).
- future-channel: **362 unit + 10 binary integration**, all pass; **2 pre-existing live-agent tests ignored**; doc tests pass (0).
- future-cli: **724 library + 2 binary unit + 11 binary integration + 2 producer/consumer integration**, all pass; doc tests pass (0).
- `node cli/tests/console-hook.mjs`: PASS against actual embedded JavaScript.
- `git diff --check`: PASS. Round3 preserved the final round2 pass, committed all14 pending files, then added bounded ingress and reran the whole batch successfully.
- Mechanical INDEX.json reconciliation: **61/61 original assigned IDs present; no missing rows; all V4 extras and w13-6 present**.

The parallel configure failure was diagnosed, not rerun away: `mcp::tests::initialize_error_without_code_says_unknown` called `point_platform_at` (writes auth.json with only base_url) without an env lock or isolated HOME, overwriting configure's fixture. Added the shared env lock and fresh test home; configure preservation then passed in **three full parallel suites**. A second unguarded run-argument test read a concurrent grpc override; it now holds the same lock and removes the override. The invalid browser transaction test object-ref fixture was corrected to canonical string refs, retaining strict production validation. These are test-harness fixes, not claimed production defects. A later clippy useless-conversion failure in stream cleanup was corrected before the final successful batch.

## gaps

- Supervisor must integrate and independently review all commits, then check combined direct consumers (agent/TUI/apps) against the appended proto fields. CLI integration already tests the actual agent Usage type and shared bridge parser; no runtime protocol rewrite or event-data dual-write removal occurred.
- Native Windows browser quoting/file-lock behavior, live Chrome/Safari default actions, and live DingTalk webhook failures were not executed on this Mac. Deterministic portable tests cover implementation boundaries; native/live claims remain unmade.
- w32-4 SSE repair is supervisor-owned; w32-5 WebUI escaping is loop-worker-owned. Exact locations/required behavior are in their rows; this branch contains no edits in agent/ or loop/.
- Latent cases retained: w25-5 reentrant router API; w26-8 corrupt data recovery, w26-9 unknown future accepted state, w26-12 impossible current producer shape; w32-3 dormant AI-card Unicode path. w26-7 unsupported legacy page rejection and w26-11 queued epoch ambiguity are not current supported-path defects. V4 N1 is not an independent defect under the verbatim-data contract.
- w26-3 preserves meaningful final-page offsets; paged-empty offset0 versus unpaged default keys remain wire-indistinguishable, with no value-level loss. No breaking scalar-to-optional migration was invented.
- Upgrade migration is documented in English/Chinese channel config docs: DingTalk sender_allowlist defaults deny-all; explicit wildcard is trust-all. Old Feishu approval cards cannot be rebound after bridge restart and report not-delivered rather than reaching a different session.

## Executed validation / rejected evidence (historical checkpoints)

Round 1 observed:
- `cargo test -p future-channel --lib`: initially 350 passed / 2 old-expectation failures. After additions/corrections, **355 passed** at 13:02 (before final small changes).
- `cargo test -p future-rpc`: **114 unit + 5 integration passed** at 12:56.
- CLI lib first run: 713 passed / 5 failures. Corrected two timeout-diagnostic races and old click/balance expectations. Untouched configure test failed parallel once, then **passed isolated** at 13:04; this is not proof the flake is repaired. Historical pending status; all additions subsequently passed the 16:13:41 full batch.
- Formatting applied to touched crates. `git diff --check` passes at round-2 resumption.
- Combined clippy attempt timed out after 120s during shared target lock/rebuild; **NOT a pass**. Round2 uses serialized check-rust.py with long timeout.
- No native Windows, real Chrome/Safari/DingTalk, or external credential tests performed. Tests use mock HTTP/gRPC and temporary paths. No production agent started/killed.

## Round2 validation checkpoint (15:36 CST)

- Commits: `dc642044` (preserved round1), `d9bff2ab` (arrival order/browser transactions).
- Serialized `check-rust.py future-rpc future-channel future-cli`: RPC fmt/clippy/full tests PASS (115 unit + 1 additive-compat + 5 wire integration); channels fmt/clippy/full tests PASS (358 unit + 10 binary; 2 live-agent integration tests remain intentionally ignored). CLI fmt/clippy PASS; full test stopped at library tests: 719 pass / 3 fail.
- Failures: new browser transaction fixture used invalid object refs instead of canonical string selectors (correcting fixture, not weakening validation); pre-existing configure preservation failed parallel again (root cause subsequently identified and fixed; see final results); unguarded run-argument test read a concurrently changed grpc env (corrected test to hold shared env lock). CLI integration/doc tests did not run in this failed checkpoint batch, but subsequently passed in the final batch.
- Additive proto fields implemented and regenerated: ToolStart phase=4/tc_index=5, ToolDelta snapshot=4, AgentEnd truncation_json=6, UsageEvent stop_reason=2, UsageInfo reasoning_tokens=7/provider_metadata_json=8. No reused numbers; original event data remains byte-stable. Fixtures come from actual agent wire projection, are protobuf-encoded/decoded, and cover snapshot true AND false. Frozen old ToolStart decoder proves bidirectional compatibility. New CLI integration uses the actual agent Usage type plus bridge consumer; both integration tests subsequently PASS.

## Checkpoint / integration notes

Round2 first action inspected status/diff: 34 tracked code files + ledger + console-hook regression preserved in checkpoint **dc642044**. Infrastructure HTTP500 interruption was acknowledged; no reset or deadline extension. At 14:47, channels **358 library tests passed** (including new ordered-arrival and stale-generation tests), and `node cli/tests/console-hook.mjs` passed against the actual embedded script. Subsequent round2 changes implement per-conversation ingress queues, generations assigned before spawn, DingTalk FIFO dedup, browser OS-locked read/modify/write sites and atomic config publication; browser transaction regression subsequently PASS. Removed unused imports exposed by callback migration. Proto additions are append-only; no existing field number changed. Public channel API `download_resource` now takes explicit byte limit; only in-crate callers found and updated. DingTalk configuration migration: populate `sender_allowlist`; empty denies all, wildcard explicit trust-all. Approval bindings are memory-only and origin-scoped: after bridge restart stale cards report not-delivered rather than approving an unrelated current session. EOF/transport cleanup, arrival ordering, browser transactions and typed event parity have since been implemented and passed the full batch; the remaining boundaries are listed below.
