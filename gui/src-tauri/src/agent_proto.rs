// Generated gRPC bindings live in the future-rpc crate (single codegen
// owner; typed-RPC milestone). Re-exported under the historical paths so
// call sites keep their `agent_proto::...` / `crate::agent_proto::...`
// references.
// Not every proto message is constructed by the GUI (e.g. server-only types),
// so silence dead-code warnings for the whole module.
#![allow(dead_code)]

pub use future_rpc::proto::future_agent_client::FutureAgentClient;
pub use future_rpc::proto::*;
