//! Generated gRPC client code from proto/future.proto (tonic/prost).
//!
//! Regenerated opt-in via `REGENERATE_PROTO=1 cargo build -p cli-rust` (the
//! Makefile `generate-proto` target) and checked into git, so normal builds
//! never need protoc — same convention as agent/ and channels/.

pub mod proto {
    include!("proto.rs");
}
