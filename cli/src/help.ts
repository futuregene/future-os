export function printHelp(): void {
  console.log(`Future OS CLI

Usage:
  future auth login
  future auth status
  future auth logout
  future agent start
  future agent stop
  future agent restart
  future agent status
  future tui [tui options]
`);
}
